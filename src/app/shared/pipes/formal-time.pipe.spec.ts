import { FormalTimePipe } from './formal-time.pipe';

describe('FormalTimePipe', () => {
  it('create an instance', () => {
    const pipe = new FormalTimePipe();
    expect(pipe).toBeTruthy();
  });
});
